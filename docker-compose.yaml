# version: '3.8'
# services:
#   nextjs-app:
#     build: .
#     ports:
#       - "8080:8080"
#     volumes:
#       - .:/app
#       - /app/node_modules
#     environment:
#       NODE_ENV: production

version: "3.8"
services:
  nextjs-app:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "8080:8080"
    environment:
      NODE_ENV: production
